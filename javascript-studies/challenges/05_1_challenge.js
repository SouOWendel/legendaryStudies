/**
 * 1) Crie um programa que dado um número imprima a sua tabuada. Utilizando array
 * e estrutura de repetição.
 */

const nTabuada = 7;
for (let i = 0; i <= 10; i++) {
	const multiplicacao = nTabuada * i;
	console.log(`${nTabuada} x ${i} = ${multiplicacao}`);
}
